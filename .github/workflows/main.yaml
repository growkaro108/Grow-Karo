name: CI/CD Pipeline to EC2

on:
  push:
    branches: [main]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Build Frontend
      - name: Build and Push Frontend
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/frontend:v1 ./grow-karo
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/frontend:v1

      # Build Backend
      - name: Build and Push Backend
        run: |
          # Yahan mvn clean package ki zaroorat pad sakti hai agar Dockerfile mein nahi hai
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/backend:v1 ./backend
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/backend:v1

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to EC2
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            # EC2 par login 
            cd /home/ec2-user/app
            docker login -u ${{ secrets.DOCKERHUB_USERNAME }} -p ${{ secrets.DOCKERHUB_TOKEN }}


            # Nayi image pull karna aur update karna
            sudo docker compose pull
            sudo docker compose up -d

            # Faltu images delete karna taaki storage na bhare
            sudo docker image prune -f
